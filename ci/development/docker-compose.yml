version: '2.1'
services:
  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - ../../data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=unity_dev
